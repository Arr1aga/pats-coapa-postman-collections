$kind: http-example
request:
  url: "{{GATEWAY_URL}}/admin/users"
  method: GET
  headers:
    Authorization: Bearer {{ACCESS_TOKEN}}
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      [
        {
          "id": "usr_abc123",
          "email": "coach@patscoapa.mx",
          "displayName": "Coach Test",
          "role": "coach"
        }
      ]
order: 1000
