$kind: http-request
description: Proxy → auth-service DELETE /admin/users/:id
url: "{{GATEWAY_URL}}/admin/users/:id"
method: DELETE
pathVariables:
  - key: id
    value: usr_abc123
    description: User UUID
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/Delete User.resources/examples
order: 4000
