$kind: http-request
description: Get full player profile (coach of category / own tutor / admin)
url: "{{GATEWAY_URL}}/roster/player/:id"
method: GET
pathVariables:
  id: plr_abc123
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/Get Player.resources/examples
order: 4000
