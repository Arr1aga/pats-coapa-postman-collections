$kind: http-request
description: Proxy → roster-service GET /roster/categories
url: "{{GATEWAY_URL}}/roster/categories"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/List Categories.resources/examples
order: 1000
