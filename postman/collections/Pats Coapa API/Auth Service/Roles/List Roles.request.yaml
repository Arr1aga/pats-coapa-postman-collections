$kind: http-request
description: List all active roles
url: "{{AUTH_URL}}/roles"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/List Roles.resources/examples
order: 1000
