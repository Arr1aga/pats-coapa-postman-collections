$kind: http-request
description: Returns 422 SESSION_DATE_NOT_IN_CALENDAR when the requested date is not present in the allowed_session_dates list for the category. The allowed_session_dates list is populated by the calendar service when a month is published.
url: "{{GATEWAY_URL}}/attendance/sessions/irons/2026-12-25"
method: PUT
headers:
  Authorization: Bearer {{ACCESS_TOKEN}}
  Content-Type: application/json
body:
  type: json
  content: '{"marks":[]}'
examples: ./.resources/Update Session — Date Not In Calendar — 422.resources/examples
order: 6000
