$kind: http-request
description: Stamps submitted_at on the session. Idempotent — calling again returns the already-submitted session.
url: "{{GATEWAY_URL}}/attendance/sessions/rabbits/today/submit"
method: POST
headers:
  Authorization: Bearer {{ACCESS_TOKEN}}
examples: ./.resources/Submit Session.resources/examples
order: 3000
