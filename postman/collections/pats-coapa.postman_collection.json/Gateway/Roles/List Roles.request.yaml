$kind: http-request
description: Proxy → auth-service GET /roles
url: "{{GATEWAY_URL}}/roles"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/List Roles.resources/examples
order: 1000
