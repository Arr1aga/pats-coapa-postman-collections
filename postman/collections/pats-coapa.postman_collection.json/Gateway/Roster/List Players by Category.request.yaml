$kind: http-request
description: List players in a category (coach/admin only)
url: "{{GATEWAY_URL}}/roster/players/:category"
method: GET
pathVariables:
  category: rabbits
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/List Players by Category.resources/examples
order: 2000
