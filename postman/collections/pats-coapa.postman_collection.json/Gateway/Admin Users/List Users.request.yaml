$kind: http-request
description: Proxy → auth-service GET /admin/users
url: "{{GATEWAY_URL}}/admin/users"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/List Users.resources/examples
order: 1000
