$kind: http-request
description: List all active users (requires users:read permission)
url: "{{AUTH_URL}}/admin/users"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/List Users.resources/examples
order: 1000
