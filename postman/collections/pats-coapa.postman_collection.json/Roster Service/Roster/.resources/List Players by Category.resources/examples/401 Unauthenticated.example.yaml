$kind: http-example
request:
  url: "{{ROSTER_URL}}/roster/players/:category"
  method: GET
  headers:
    Authorization: Bearer {{ACCESS_TOKEN}}
  pathVariables:
    category: rabbits
response:
  statusCode: 401
  statusText: Unauthorized
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "code": "UNAUTHENTICATED",
        "message": "Missing actor identity"
      }
order: 2000
