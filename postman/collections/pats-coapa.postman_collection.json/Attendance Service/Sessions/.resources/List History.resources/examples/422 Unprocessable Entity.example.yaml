$kind: http-example
request:
  url: "{{ATTENDANCE_URL}}/attendance/sessions/invalid_cat/history"
  method: GET
  headers:
    x-actor-id: "{{ACTOR_ID}}"
    x-actor-role: head_coach
response:
  statusCode: 422
  statusText: Unprocessable Entity
  headers:
    Content-Type: application/json
  body:
    type: json
    content: "{\"code\":\"VALIDATION_ERROR\",\"message\":\"Unknown category: 'invalid_cat'\"}"
order: 3000
