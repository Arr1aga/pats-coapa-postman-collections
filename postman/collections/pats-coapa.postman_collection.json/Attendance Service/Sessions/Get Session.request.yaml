$kind: http-request
description: Returns the attendance session for the given category and date. Use `today` or a YYYY-MM-DD date. Returns empty marks array when no session exists yet.
url: "{{GATEWAY_URL}}/attendance/sessions/rabbits/today"
method: GET
headers:
  Authorization: Bearer {{ACCESS_TOKEN}}
examples: ./.resources/Get Session.resources/examples
order: 1000
